# tsc-audit

Results repository for the corresponding CCCS WCAG audit instance. The audit
pipeline pushes report JSON to `reports/` and renders the GitHub Pages site
into `docs/`. No manual commits expected.
